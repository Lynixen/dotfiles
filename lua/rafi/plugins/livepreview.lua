return {
	'brianhuster/live-preview.nvim',
	dependencies = { 'brianhuster/autosave.nvim' }, -- Not required, but recommended for autosaving
	opts = {},
	ft = { 'html', 'markdown' }, -- Lazy load on HTML and Markdown files
}
